version: "3.9"

services:
  impostor-game:
    build: .
    container_name: impostor-game
    restart: always
    ports:
      - "3000:3000"
    networks:
      - impostor-net

networks:
  impostor-net:
    driver: bridge
